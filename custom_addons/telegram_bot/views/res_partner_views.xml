<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_form_telegram" model="ir.ui.view">
        <field name="name">res.partner.form.telegram</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="has_telegram" widget="boolean_toggle" invisible="1"/>
                <button name="action_view_telegram_users" string="Telegram" type="object"
                        class="oe_stat_button" icon="fa-telegram" invisible="not has_telegram"/>
            </xpath>
        </field>
    </record>
</odoo>

